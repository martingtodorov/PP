# PurePeptide backend — PRODUCTION dependencies
# Installed by deploy/hetzner/ansible/playbooks/deploy_backend.yml into /opt/purepeptide/venv.
#
# Only the packages the backend actually imports are listed (top-level pins; pip resolves the
# transitive tree). Do NOT paste a `pip freeze` of the development pod in here: it drags in ~90
# unrelated packages (google-*, litellm, openai, pandas, numpy, black, mypy, pytest, boto3 …) whose
# pins conflict with each other on a modern Python (google-api-core needs grpcio-status>=1.75 on
# Python 3.14) and it also pins emergentintegrations, which only exists on the Emergent index.
#
# Verified to resolve with `pip install --dry-run --python-version 3.14 --only-binary=:all:`
# (http-ece, a pywebpush dependency, is source-only and builds with build-essential).
#
# When you add a new import to backend/*.py, add the package here — the test
# backend/tests/test_requirements_portable.py fails otherwise.

# --- web framework -------------------------------------------------------------
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.12.5
email-validator==2.3.0
python-multipart==0.0.24
python-dotenv==1.2.2

# --- database ------------------------------------------------------------------
motor==3.7.1
pymongo==4.15.5

# --- auth ----------------------------------------------------------------------
PyJWT==2.12.1
bcrypt==4.1.3

# --- outbound HTTP / integrations ----------------------------------------------
httpx==0.28.1
requests==2.33.1
resend==2.42.0
anthropic==1.2.0

# --- media / files -------------------------------------------------------------
pillow==12.2.0
openpyxl==3.1.5

# --- web push ------------------------------------------------------------------
pywebpush==2.5.0
